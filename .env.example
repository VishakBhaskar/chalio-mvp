# Frontend Environment Variables

# Backend API URL (optional - only needed for local development with separate ports)
# For single service deployment on Railway, leave this empty (uses same origin)
# For local development with React on port 3000 and server on port 3001:
REACT_APP_BACKEND_URL=http://localhost:3001

# Retell Agent ID (optional - can be passed from backend)
REACT_APP_RETELL_AGENT_ID=your_agent_id_here
