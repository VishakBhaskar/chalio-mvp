# Frontend Environment Variables

# Backend API URL (required for production)
# For local development, this defaults to http://localhost:3001
# For production, set this to your Railway backend URL (e.g., https://your-backend.railway.app)
REACT_APP_BACKEND_URL=http://localhost:3001

# Retell Agent ID (optional - can be passed from backend)
REACT_APP_RETELL_AGENT_ID=your_agent_id_here
